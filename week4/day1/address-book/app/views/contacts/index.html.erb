<!DOCTYPE html>
<html>
<head>
  <title></title>
</head>
<body>
  <h1>Address Book</h1>
  <ul>
    <% @contacts.each do |contact| %>
    <li><a href="/contact/<%= contact.name %>"><%= "#{contact.name.capitalize}" %></a></li>
    <% end %>
  </ul>
  <a href="/contacts/new">Add new contact</a>
</body>
</html>