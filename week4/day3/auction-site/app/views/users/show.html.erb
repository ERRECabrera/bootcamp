<h1><%= @user.name %></h1>
<p><%= @user.email %></p>
<hr>
<ul>
<% @products.each do |product| %>
    <li><%= link_to(product.title, user_product_path(@user.id,product.id)) %></li>
<% end %>
</ul>
<%= link_to("Add product", new_user_product_path(@user.id)) %> / 
<%= link_to("go back to the users list", users_path()) %>
